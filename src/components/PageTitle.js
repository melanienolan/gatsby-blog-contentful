import React from "react"

const PageTitle = ({ children }) => {
  return (
    <h2 className="my-12 text-purple-500 font-bold text-4xl text-center">
      {children}
    </h2>
  )
}

export default PageTitle
